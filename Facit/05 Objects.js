// 1. Alert "Richard" by extracting information from the person object.
const person = {
    firstName: "Richard",
    lastName: "Chalk"
};

alert(person.firstName);

// 2. Add the following property and value to the person object: country: Sweden.
// console.log() Richards country
const personSe = {
    firstName: "Richard",
    lastName: "Chalk",
    country: "Sweden"
};

console.log(personSe.country)

// 3. Create an object called person with name = Stefan, age = 52.
// Then, access the object to alert("Stefan is 52").
const personAge = {
    name: "Stefan",
    age: 52
};
alert(personAge.name + " is " + personAge.age);

